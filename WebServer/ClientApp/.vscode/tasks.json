{
  "version": "2.0.0",
	"tasks": [
    {
      "label": "Run Unit Tests",
      "command": "gulp",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceRoot}/client"
      }
    }
  ]
}