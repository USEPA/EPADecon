{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Full Application",
      "type": "chrome",
      // "preLaunchTask": "Run API Server",
      "request": "launch",
      "url": "http://localhost:5005/#",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
